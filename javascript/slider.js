$(document).ready(function () {
  $(".main-carousel").flickity({
    // options
    cellAlign: "left",
    contain: true,
    draggable: true,
    pageDots: false,
    setGallerySize: false,
  });
});
